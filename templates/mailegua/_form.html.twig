{% form_theme form 'bootstrap_3_layout.html.twig' %}
{{ form_start(form) }}

    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ form_row(form.erabiltzailea) }}
            {{ form_row(form.bizikleta) }}
        </div>
        <div class="col-md-2"></div>
    </div>

    <hr>
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ form_row(form.startGunea) }}
            {{ form_row(form.dateStart) }}
        </div>
        <div class="col-md-2"></div>
    </div>

    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ form_row(form.ibilbidea) }}
            {{ form_row(form.eguraldia) }}
        </div>
        <div class="col-md-2"></div>
    </div>

    <hr>

    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ form_row(form.endGunea) }}
            {{ form_row(form.dateEnd) }}
        </div>
        <div class="col-md-2"></div>
    </div>

    <hr>

    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ form_row(form.zigorra) }}
            {{ form_row(form.matxura) }}
        </div>
        <div class="col-md-2"></div>
    </div>

    <div style="display: none;">
        {{ form_row(form.udala) }}
        <button id="crudSubmitButton" class="btn">{{ button_label|default(' Savesss') }}</button>
    </div>

{{ form_end(form)}}
