{% extends 'base.html.twig' %}

{% block title %}Ibilbidea{% endblock %}

{% block body %}
    <h1>Ibilbidea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ibilbidea.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ibilbidea.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ ibilbidea.createdAt ? ibilbidea.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ ibilbidea.updatedAt ? ibilbidea.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ibilbidea_index') }}">back to list</a>

    <a href="{{ path('app_ibilbidea_edit', {'id': ibilbidea.id}) }}">edit</a>

    {{ include('ibilbidea/_delete_form.html.twig') }}
{% endblock %}
