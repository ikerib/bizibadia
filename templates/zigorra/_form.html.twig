{{ form_start(form) }}
    {{ form_row(form.name) }}
<div style="display: none;">
    {{ form_row(form.udala) }}
    <button id="crudSubmitButton" class="btn">{{ button_label|default(' Savesss') }}</button>
</div>
{{ form_end(form) }}
