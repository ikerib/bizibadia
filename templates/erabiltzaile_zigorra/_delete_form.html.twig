<form method="post" action="{{ path('app_erabiltzaile_zigorra_delete', {'id': erabiltzaile_zigorra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ erabiltzaile_zigorra.id) }}">
    <button class="btn">Delete</button>
</form>
